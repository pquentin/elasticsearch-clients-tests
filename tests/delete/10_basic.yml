---
"Basic":

 - do:
      index:
          index:  test_serverless
          id:     "1"
          body:   { foo: bar }

 - match:   { _version: 1 }

 - do:
      delete:
          index:  test_serverless
          id:     "1"

 - match: { result: deleted }